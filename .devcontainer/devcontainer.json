{
  "name": "Codespace Tailscale",

  "runArgs": [
    "--device=/dev/net/tun",
    "--cap-add=NET_ADMIN"
  ],

  "features": {
    "ghcr.io/tailscale/codespace/tailscale:latest": {}
  },

  "postCreateCommand": "sudo tailscale up --authkey=${TS_AUTH_KEY} --advertise-exit-node"
}
